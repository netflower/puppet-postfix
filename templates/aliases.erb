###########################################################################################
### This file is managed by puppet, and is refreshed regularly. Edit at your own peril! ###
###########################################################################################

# See man 5 aliases for format
postmaster:    root
<% @aliases.sort_by {|k, v| k}.each do |key, value| -%>
<%= key %>: <%= [value].flatten.join(', ') %>
<% end -%>
